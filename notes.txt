10 -may 
sequence ,iteration ,selection 
in python the bool True and False (capital letter first)
c doesnt support the bool values so we have to inculde the <stdbool.h> library
the main function is the client 
the helper functions are the servers 
filter->throw u out of the task 
updater->chaneg the values as per the need and performs the task
the main goal must to to resue the helper function not the main function 
signed->31 bit (+ve, - ve)
unsigned->32 bit (only +ve)


---



input twi types
bufffer 
loop->time compexilty
ne malloc->space compexilty 
boiler plate code
in c bool datatype is not there
naked function 

24 may 2026
fro the factors exculde the the factor itself and the 1 
What return Actually Does

return does two things:

ends the current method
optionally sends a value back

So when this runs:

return false;

Java immediately leaves:

CheckPrime()

Entire function stops.

Not just loop.

reverse number 
when there is 0 at last it is not preserved so for that =>
int
stores numeric values only.
Mathematically:
092=92
Leading zeros are ignored in decimal integers.

 r value -> resident value
 iValue      // 11
&iValue     // 100

iPtr        // 100 (address of iValue)
*iPtr       // 11

&iPtr       // 200 (address of the pointer itself)
never add \n in scanf

in c ->8 digit one number 
in python 100 digit one number


in java in the function is is by default call by reference 
delete-> deallocate the memory of the first element only 
delete[]->deallocate the memory of the complete array 
so and this is what we do 

//a->97
A->65
0->48

//("%%") in c we cant print  % so we use this to print % (as % in c is format specifier)

-java
length in arrray is property
length() for string method

string is the topic which is different in all languags
charAt()is heavy 
toCharArray()

custom lib ->C:\Users\hp\OneDrive\myspace\logic_building\StringX.java

-endien ->big end ,little end(egg example)

-numbering system->binaray,decimal ,hex,oct (not much used used for embeding)

-this both are different 
~->unary 
!->not 

-byte->nibble->bits

-BIT->Binary digIT(BIT)

website->penjee

