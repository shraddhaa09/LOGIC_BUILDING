7**First - Array is stored in memory**
- You create array - Java finds space in memory
- Memory gives a starting address
4 Let's say address = 100
Y That's called Base Address

**How Java finds any element**
Formula= Base Address + (index x size)
arr[0]=100+ (0x 4)=100
arr[1]=100+(1x4)=104
- arr[2] = 100 + (2 x 4) = 108
, Direct calculation - no searching

**What if array started from 1**
4 arr[1] = 100 + (1-1 x 4) = 100
○◦ Extra -1 calculation every single time
- Millions of array operations daily
4 That -1 wastes CPU time
◦o So Java removed it -started from 0

**Simple analogy**
- Building starts from Ground Floor = 0
- Not Floor 1
⁃ Lift calculation is easier from 0
Same logic in memory

**Why this impresses interviewer**
- Most say "ust a convention"
@o Wrong - it's a performance decision
4 0 index =faster memory calculation
Every language does this for same reasony10 -may 
sequence ,iteration ,selection 
in python the bool True and False (capital letter first)
c doesnt support the bool values so we have to inculde the <stdbool.h> library
the main function is the client 
the helper functions are the servers 
filter->throw u out of the task 
updater->chaneg the values as per the need and performs the task
the main goal must to to resue the helper function not the main function 
signed->31 bit (+ve, - ve)
unsigned->32 bit (only +ve)


---



input twi types
bufffer 
loop->time compexilty
ne malloc->space compexilty 
boiler plate code
in c bool datatype is not there
naked function 

24 may 2026
fro the factors exculde the the factor itself and the 1 
What return Actually Does

return does two things:

ends the current method
optionally sends a value back

So when this runs:

return false;

Java immediately leaves:

CheckPrime()

Entire function stops.

Not just loop.

reverse number 
when there is 0 at last it is not preserved so for that =>
int
stores numeric values only.
Mathematically:
092=92
Leading zeros are ignored in decimal integers.

 r value -> resident value
 iValue      // 11
&iValue     // 100

iPtr        // 100 (address of iValue)
*iPtr       // 11

&iPtr       // 200 (address of the pointer itself)
never add \n in scanf

in c ->8 digit one number 
in python 100 digit one number


in java in the function is is by default call by reference 
delete-> deallocate the memory of the first element only 
delete[]->deallocate the memory of the complete array 
so and this is what we do 

//a->97
A->65
0->48

//("%%") in c we cant print  % so we use this to print % (as % in c is format specifier)

-java
length in arrray is property
length() for string method

string is the topic which is different in all languags
charAt()is heavy 
toCharArray()

custom lib ->C:\Users\hp\OneDrive\myspace\logic_building\StringX.java

-endien ->big end ,little end(egg example)

-numbering system->binaray,decimal ,hex,oct (not much used used for embeding)

-this both are different 
~->unary 
!->not 

-byte->nibble->bits

-BIT->Binary digIT(BIT)

website->penjee,devtools cal

-No. C supports only pass by value. When we want a function to modify the original variable, we pass its address using a pointer. The pointer is still passed by value, but it lets the function access and modify the original data. This is often called "pass by address," not true pass by reference.

**First - Array is stored in memory**
- You create array - Java finds space in memory
- Memory gives a starting address
4 Let's say address = 100
Y That's called Base Address

**How Java finds any element**
Formula= Base Address + (index x size)
arr[0]=100+ (0x 4)=100
arr[1]=100+(1x4)=104
- arr[2] = 100 + (2 x 4) = 108
, Direct calculation - no searching

**What if array started from 1**
4 arr[1] = 100 + (1-1 x 4) = 100
○◦ Extra -1 calculation every single time
- Millions of array operations daily
4 That -1 wastes CPU time
◦o So Java removed it -started from 0

**Simple analogy**
- Building starts from Ground Floor = 0
- Not Floor 1
⁃ Lift calculation is easier from 0
Same logic in memory

**Why this impresses interviewer**
- Most say "ust a convention"
@o Wrong - it's a performance decision
4 0 index =faster memory calculation
Every language does this for same reason
E JDK VS JRE Vs JVM Java Interview Favorite

If you remember just one line, remember this:

JVM= Runs Java bytecode
V JRE = JVM+ libraries needed to run Java apps
V JDK = JRE+ developer tools to write/compile Java
code

Simple way to remember

* JVM - executes .class files
* JRE runs Java programs
* JDK -+ develops + runs Java programs

Formula:

JDK = JRE + Development Tools
JRE = JVM+ Core Libraries
